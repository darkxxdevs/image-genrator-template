import { Rubik_Iso, Cinzel } from 'next/font/google'



export const rbFont = Rubik_Iso({
    subsets: ['latin'],
    weight: '400'
})

export const cin = Cinzel({
    subsets: ['latin'],
    weight: '400'

})